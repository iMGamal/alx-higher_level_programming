#!usr/bin/node
class Rectangle {
  constructor(w, h); {
    this.width = w;
    this.height = h;
    this.empty = {};
  }
  const checkerw = parseInt(width);
  const checkerh = parseInt(height);
  if (isNaN(checkerw)) {
    width = empty;
  }
  if (isNan(checkerh)) {
    height = empty
  }
}
module.exports = Rectangle;
